'use strict';

var wiring = function(config, app) {
    console.log(app);
    //var servicesLocation = config.services;
    //require(config.services.location + "/" + config.services.user)(app.router);
};
exports = module.exports = wiring;